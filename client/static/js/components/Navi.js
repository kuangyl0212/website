'use strict';
var React = require('react');

export default class Navigator extends React.Component {
    render() {
        return (
            <<nav>
                <a>首页</a>
                <a></a>
            </nav>
        )
    }
}

